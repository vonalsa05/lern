# Лабораторная работа 08: Продвинутые выборки (CTE, Представления, Материализованные представления)

## Оглавление
- [Предварительные требования](#предварительные-требования)
- [Часть 1: CTE (Common Table Expressions)](#часть-1-cte-common-table-expressions)
- [Часть 2: Представления (VIEW)](#часть-2-представления-view)
- [Часть 3: Материализованные представления (MATERIALIZED VIEW)](#часть-3-материализованные-представления)
- [Вопросы для самопроверки](#вопросы-для-самопроверки)

Время: ~30 мин | Сложность: 3/5

Цель: научиться писать сложные аналитические запросы более читаемым образом через `WITH`, а также понять разницу между обычным `VIEW` и кэшируемым `MATERIALIZED VIEW`.

---

## Предварительные требования
База данных `pagila` и все необходимые таблицы уже созданы и наполнены данными при запуске окружения. Никаких дополнительных действий для инициализации БД выполнять не требуется.

---

## Часть 1: CTE (Common Table Expressions)

Когда запросы становятся сложными и обрастают вложенными подзапросами, они быстро теряют читабельность.
Для решения этой проблемы используется оператор `WITH`, позволяющий создавать временные "виртуальные таблицы" (CTE), которые существуют только во время выполнения запроса.

**Без CTE (вложенный запрос):**
```sql
SELECT avg_amount 
FROM (
    SELECT customer_id, AVG(amount) as avg_amount
    FROM payment 
    GROUP BY customer_id
) AS order_stats 
WHERE avg_amount > 5;
```

**С использованием CTE (гораздо читаемее):**
```sql
WITH order_stats AS (
    SELECT customer_id, AVG(amount) as avg_amount
    FROM payment 
    GROUP BY customer_id
)
SELECT avg_amount 
FROM order_stats 
WHERE avg_amount > 5;
```
*Подсказка: CTE могут быть цепочечными! Можно написать `WITH cte1 AS (...), cte2 AS (...) SELECT ...`*

---

## Часть 2: Представления (VIEW)

Если один и тот же сложный запрос (например, отчет со множеством джоинов) нужно выполнять часто из разных мест приложения, его можно сохранить в базе данных как "Представление" (`VIEW`).

```sql
CREATE VIEW user_order_summary AS
SELECT 
    c.customer_id, 
    c.first_name, 
    COUNT(p.payment_id) as total_payments
FROM customer c
LEFT JOIN payment p ON c.customer_id = p.customer_id
GROUP BY c.customer_id, c.first_name;
```

Теперь мы можем делать запросы к этому представлению так же, как к обычной таблице:
```sql
SELECT * FROM user_order_summary WHERE total_payments > 5;
```

**Важно:** Обычный `VIEW` не хранит данные физически! Каждый раз, когда вы делаете `SELECT * FROM view`, база данных "под капотом" выполняет исходный сложный запрос. Это не ускоряет работу, а лишь упрощает синтаксис.

---

## Часть 3: Материализованные представления

Если "тяжелый" аналитический запрос выполняется слишком долго (например, считаются продажи за весь год), обычный `VIEW` не поможет. В этом случае на помощь приходит `MATERIALIZED VIEW`.

Материализованное представление физически кэширует (сохраняет) результаты выполнения запроса на диск.

```sql
CREATE MATERIALIZED VIEW monthly_orders_report AS
SELECT 
    date_trunc('month', payment_date) as report_month,
    COUNT(payment_id) as total_payments
FROM payment
GROUP BY date_trunc('month', payment_date);
```

Скорость `SELECT * FROM monthly_orders_report` будет мгновенной, так как данные уже посчитаны! 
**DevOps-инсайт:** Но есть нюанс — данные в материализованном представлении могут "протухать". При добавлении новых платежей отчет не обновится автоматически. Его нужно обновлять явно (например, по cron-расписанию или через триггер):

```sql
-- Полное перестроение кэша (блокирует чтение)
REFRESH MATERIALIZED VIEW monthly_orders_report;

-- Обновление без блокировки чтения (требует уникального индекса на VIEW!)
REFRESH MATERIALIZED VIEW CONCURRENTLY monthly_orders_report;
```

---

## Вопросы для самопроверки
1. Можно ли вставить данные в CTE с помощью `INSERT INTO my_cte ...`?
2. В чем фундаментальное отличие `VIEW` от `MATERIALIZED VIEW` с точки зрения нагрузки на I/O подсистему диска?
3. Зачем может понадобиться использовать `REFRESH MATERIALIZED VIEW CONCURRENTLY`, и почему он недоступен "из коробки" без индексов?
