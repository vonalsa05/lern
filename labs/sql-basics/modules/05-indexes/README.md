# Лабораторная работа 05: Индексы и оптимизация запросов (EXPLAIN ANALYZE)

## Оглавление
- [Часть 1: Планировщик запросов и EXPLAIN](#часть-1-планировщик-запросов-и-explain)
- [Часть 2: EXPLAIN ANALYZE и логирование буферов (BUFFERS)](#часть-2-explain-analyze-и-логирование-буферов-buffers)
- [Часть 3: Индексы (B-Tree) и их создание](#часть-3-индексы-b-tree-и-их-создание)
- [Часть 4: Продвинутые индексы для DevOps (Partial, Covering)](#часть-4-продвинутые-индексы-для-devops-partial-covering)
- [Часть 5: Мониторинг неиспользуемых индексов](#часть-5-мониторинг-неиспользуемых-индексов)
- [Часть 6: Troubleshooting — почему индекс не используется?](#часть-6-troubleshooting--почему-индекс-не-используется)
- [Вопросы для самопроверки](#вопросы-для-самопроверки)

Время: ~40 мин | Сложность: 4/5

Цель: научиться глубоко анализировать производительность SQL-запросов, читать планы выполнения с метриками дискового ввода-вывода (I/O), оптимизировать медленные выборки с помощью составных, частичных и покрывающих индексов, а также находить «мусорные» индексы в СУБД.

---

## Часть 1: Планировщик запросов и EXPLAIN

Прежде чем выполнить любой SQL-запрос, СУБД передает его **планировщику (optimizer)**. Планировщик строит дерево возможных путей выполнения запроса, оценивает их «стоимость» (cost) на основе системной статистики таблиц и выбирает самый дешевый план.

Команда `EXPLAIN` показывает план выполнения запроса, выбранный планировщиком, **без фактического запуска самого запроса**. Это безопасно запускать на продакшене для анализа тяжелых запросов.

```sql
EXPLAIN SELECT * FROM customer WHERE email = 'jared.ely@sakilacustomer.org';
```

Вывод команды `EXPLAIN` содержит:
* `Seq Scan` (Sequential Scan) — последовательное чтение всей таблицы с диска.
* `cost=0.00..2.50` — оценка стоимости (cost). Первая цифра (0.00) — стоимость получения первой строки, вторая (2.50) — стоимость получения всех строк. Это безразмерные единицы стоимости (по умолчанию 1 unit = чтение одной страницы с диска).
* `rows=1` — планируемое количество строк в результате.
* `width=34` — средняя ширина строки в байтах.

---

## Часть 2: EXPLAIN ANALYZE и логирование буферов (BUFFERS)

Для точной диагностики одной команды `EXPLAIN` часто недостаточно, так как планировщик может ошибаться в оценках (например, если статистика БД устарела). 

Команда `EXPLAIN ANALYZE` **выполняет** запрос в базе данных, замеряет точное время работы каждого шага и выводит как планируемые показатели, так и реальные (`actual time`, `loops`, `rows`).

Для DevOps-инженера критически важна метрика **дискового I/O**. Чтобы увидеть её, используют опцию `BUFFERS`:
```sql
EXPLAIN (ANALYZE, BUFFERS) SELECT * FROM customer WHERE email = 'jared.ely@sakilacustomer.org';
```

Пример вывода:
```text
Seq Scan on customer  (cost=0.00..2.50 rows=1 width=34) (actual time=0.015..0.038 rows=1 loops=1)
  Filter: (email = 'jared.ely@sakilacustomer.org'::text)
  Rows Removed by Filter: 598
  Buffers: shared hit=2
Planning Time: 0.082 ms
Execution Time: 0.055 ms
```
Здесь мы видим новую секцию **`Buffers`**:
* `shared hit=2` — 2 блока данных (страницы памяти по 8 КБ) были считаны из кэша оперативной памяти PostgreSQL (shared buffers).
* Если бы в кэше этих данных не оказалось, мы бы увидели `read=X` (чтение напрямую с диска, что работает значительно медленнее).
* **Главное правило оптимизации:** Снижать количество затрагиваемых буферов (блоков) при выполнении запроса.

---

## Часть 3: Индексы (B-Tree) и их создание

Чтобы не сканировать таблицу целиком, создают **индексы**. Индекс — это вспомогательная структура данных (по умолчанию сбалансированное дерево — B-Tree), которая хранит отсортированные значения колонки и ссылки на строки в основной таблице.

1. **Автоматические индексы:**
   Первичные ключи (`PRIMARY KEY`) и ограничения уникальности (`UNIQUE`) автоматически создают индексы в PostgreSQL. Нам не нужно вручную создавать индекс для колонок вроде `customer_id` или уникального поля (например, `email`, если бы на нем было задано ограничение `UNIQUE`). *Примечание: в демонстрационной БД pagila на колонке `email` таблицы `customer` ограничение `UNIQUE` отсутствует, пример приведен для иллюстрации.*

2. **Ручное создание индексов:**
   Для колонок, по которым часто идет фильтрация (`WHERE`) или соединение (`JOIN`), создают индексы вручную:
   ```sql
   CREATE INDEX idx_customer_create_date ON customer(create_date);
   ```

3. **Составные индексы (Composite Indexes):**
   Если в `WHERE` часто фильтруются две колонки одновременно через логический оператор `AND`, эффективнее создать один составной индекс:
   ```sql
   CREATE INDEX idx_film_rating_rate ON film(rating, rental_rate);
   ```

---

## Часть 4: Продвинутые индексы для DevOps (Partial, Covering)

Существуют специализированные типы индексов, которые позволяют экономить оперативную память сервера и дисковое пространство.

### 1. Частичные индексы (Partial Indexes)
Если вы часто делаете запросы только по определенному подмножеству данных (например, ищете только невозвращенные аренды или активных клиентов), нет смысла индексировать всю таблицу. Можно создать индекс с условием `WHERE`:

```sql
CREATE INDEX idx_rental_not_returned ON rental(customer_id) 
WHERE return_date IS NULL;
```
* **Плюсы:** Такой индекс занимает в разы меньше места на диске и быстрее обновляется при вставках/обновлениях.

### 2. Покрывающие индексы (Covering Indexes) и `Index Only Scan`
Обычно при использовании индекса СУБД делает сначала поиск по дереву индекса (`Index Scan`), а затем идет в саму таблицу на диске, чтобы прочитать остальные запрашиваемые колонки.
Если мы добавим дополнительные колонки в индекс с помощью секции `INCLUDE`, база данных сможет прочитать все данные прямо из индекса, не обращаясь к таблице на диске. Это называется **`Index Only Scan`**.

```sql
CREATE INDEX idx_film_covering ON film(rating) 
INCLUDE (title, rental_rate);
```
При выполнении запроса:
```sql
SELECT title, rental_rate FROM film WHERE rating = 'G';
```
СУБД выполнит `Index Only Scan` и прочитает 0 блоков основной таблицы (все данные будут взяты из индекса).

---

## Часть 5: Мониторинг неиспользуемых индексов

Каждый индекс замедляет операции `INSERT`, `UPDATE` и `DELETE`, так как СУБД вынуждена обновлять данные индекса при каждом изменении. 

DevOps-инженер должен уметь находить индексы, которые были созданы, но не используются приложениями. Для этого в PostgreSQL используется системная вьюха `pg_stat_user_indexes`:

```sql
SELECT 
    schemaname,
    relname AS table_name,
    indexrelname AS index_name,
    idx_scan AS number_of_scans
FROM pg_stat_user_indexes
WHERE idx_scan = 0 AND indexrelname NOT LIKE '%_pkey';
```
* Если колонка `idx_scan` равна `0` (или очень мала), этот индекс бесполезен, тратит дисковое пространство и замедляет запись. Его можно удалить.

---

## Часть 6: Troubleshooting — почему индекс не используется?

### Сценарий 1: Индекс создан, но СУБД всё равно делает Seq Scan
Вы создали индекс на колонку `create_date` в таблице `customer`. Но при запуске `EXPLAIN ANALYZE` видите `Seq Scan`.

**Причина и решение:**
1. **Маленький размер таблицы.** Если в таблице всего 600 строк (как в нашей БД pagila), планировщик решает, что прочитать несколько блоков данных целиком быстрее, чем сначала читать индексный файл, а потом переходить к таблице. Чтение индекса добавляет накладные расходы. В таких случаях `Seq Scan` — это абсолютно нормальный выбор планировщика.
2. **Как проверить?** Для принудительного тестирования индекса на маленьких таблицах в рамках сессии можно отключить последовательные сканирования:
   ```sql
   SET enable_seqscan = off;
   EXPLAIN ANALYZE SELECT * FROM customer WHERE create_date = '2006-02-14';
   ```

### Сценарий 2: Использование функций «убивает» индекс
Разработчик написал запрос:
```sql
SELECT * FROM customer WHERE EXTRACT(YEAR FROM create_date) = 2006;
```
Этот запрос делает `Seq Scan`, хотя индекс на `create_date` существует.

**Причина и решение:**
Индекс построен по значениям колонки `create_date`, а не по результатам функции `EXTRACT`. Чтобы индекс заработал, нужно переписать запрос на поиск по диапазону дат:
```sql
SELECT * FROM customer WHERE create_date BETWEEN '2006-01-01' AND '2006-12-31';
```
Либо создать специальный функциональный индекс:
```sql
CREATE INDEX idx_customer_create_year ON customer ((EXTRACT(YEAR FROM create_date)));
```

---

## Вопросы для самопроверки
1. Зачем использовать опцию `BUFFERS` в `EXPLAIN ANALYZE`?
2. В каких случаях вы предпочтете создать частичный индекс (Partial Index) вместо обычного?
3. Что произойдет с производительностью вставок (`INSERT`), если мы создадим 15 индексов на таблицу из 5 колонок?
4. Чем `Index Only Scan` отличается от `Index Scan`? Какое ключевое слово включает эту оптимизацию?
