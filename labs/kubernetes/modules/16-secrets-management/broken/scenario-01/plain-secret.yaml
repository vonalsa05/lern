# АНТИ-ПАТТЕРН: обычный Secret в git. base64 — НЕ шифрование (обратимо одной командой).
# Закоммитив это, вы фактически опубликовали пароль. См. README broken-сценария.
apiVersion: v1
kind: Secret
metadata: { name: leaked-creds, namespace: lab }
type: Opaque
data:
  password: U3VwZXJTZWNyZXQxMjM=   # echo -n SuperSecret123 | base64  -> мгновенно обратимо
