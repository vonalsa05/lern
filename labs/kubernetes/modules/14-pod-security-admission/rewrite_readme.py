import re

content = """# Лабораторная работа 14: Pod Security и Admission Control (PSA + ValidatingAdmissionPolicy)

## Оглавление
<!-- TOC -->
- [Предварительные требования](#предварительные-требования)
- [Стартовая проверка](#стартовая-проверка)
- [Часть 1: Pod Security Admission (PSA)](#часть-1-pod-security-admission-psa)
  - [Теория для изучения перед частью: Зачем нам изолировать поды?](#теория-для-изучения-перед-частью-зачем-нам-изолировать-поды)
  - [PSA vs PSP — почему PSP убрали и что взамен](#psa-vs-psp--почему-psp-убрали-и-что-взамен)
  - [1.1 restricted: good проходит, bad отклоняется](#11-restricted-good-проходит-bad-отклоняется)
  - [1.2 Стратегия безопасной миграции: warn и audit](#12-стратегия-безопасной-миграции-warn-и-audit)
- [Часть 2: ValidatingAdmissionPolicy (CEL)](#часть-2-validatingadmissionpolicy-cel)
  - [Теория для изучения перед частью](#теория-для-изучения-перед-частью)
  - [2.1 Запрет :latest](#21-запрет-latest)
  - [2.2 Проверка наличия обязательных меток (labels)](#22-проверка-наличия-обязательных-меток-labels)
- [Часть 3: Policy engines (Kyverno / OPA Gatekeeper) — обзор](#часть-3-policy-engines-kyverno--opa-gatekeeper--обзор)
  - [Теория для изучения перед частью](#теория-для-изучения-перед-частью-1)
  - [Сравнение и примеры политик](#сравнение-и-примеры-политик)
- [Часть 4: Troubleshooting](#часть-4-troubleshooting)
  - [Теория: порядок admission-конвейера (что за чем выполняется)](#теория-порядок-admission-конвейера-что-за-чем-выполняется)
  - [Теория: как читать отказ admission (кто именно отклонил)](#теория-как-читать-отказ-admission-кто-именно-отклонил)
  - [Инцидент 1: под отклонён PSA (restricted)](#инцидент-1-под-отклонен-psa-restricted)
  - [Инцидент 2: легитимный под не создаётся из-за VAP](#инцидент-2-легитимный-под-не-создается-из-за-vap)
  - [Инцидент 3: PSA «не срабатывает»](#инцидент-3-psa-не-срабатывает)
  - [Инцидент 4: Конфликт Mutating Webhook и PSA](#инцидент-4-конфликт-mutating-webhook-и-psa)
- [Проверка модуля](#проверка-модуля)
- [Финальная карта ресурсов модуля](#финальная-карта-ресурсов-модуля)
- [Теоретические вопросы (итоговые)](#теоретические-вопросы-итоговые)
- [Практические задания (отработка)](#практические-задания-отработка)
- [Шпаргалка](#шпаргалка)
- [Чему вы научились](#чему-вы-научились)
- [Уборка](#уборка)
<!-- /TOC -->

> ⏱ время ~45 мин · сложность 4/5 · пререквизиты: Трек 1 и Трек 3

Цель: научиться запрещать небезопасные поды НА ВХОДЕ (admission), пока они не
попали в кластер — через встроенный Pod Security Admission (профили
privileged/baseline/restricted) и ValidatingAdmissionPolicy (кастомные правила на
CEL). К концу модуля вы включаете restricted на namespace, читаете отказ
admission и пишете свою политику без установки внешних движков.

> Всё в этом модуле — **встроено в Kubernetes** (PSA GA с 1.25, VAP GA с 1.30),
> доп. установки не требуется. Kyverno/OPA — только для сложных кейсов (обзор в
> Части 3).

---

## Предварительные требования

```bash
# kubeconfig нашего кластера (Kubespray); на другом стенде — свой путь/контекст
export KUBECONFIG=/root/.kube/kubespray.conf
kubectl get ns lab >/dev/null 2>&1 || kubectl create ns lab
kubectl delete ns lab-restricted --ignore-not-found 2>/dev/null
kubectl version -o json 2>/dev/null | grep -i gitVersion | head -1   # нужен >=1.30 для VAP
```

## Стартовая проверка

Убедитесь, что кластер доступен и ноды находятся в статусе Ready:
```bash
kubectl get nodes
```

---

## Часть 1: Pod Security Admission (PSA)

### Теория для изучения перед частью: Зачем нам изолировать поды?

В отличие от виртуальных машин (где каждая ВМ имеет свое ядро операционной системы), контейнеры **делят одно общее ядро Linux** с хостовой нодой (worker node). Если процесс внутри контейнера запущен от имени пользователя `root` (UID 0) и контейнер не ограничен в правах (capabilities), злоумышленник, взломавший приложение, может:
1. Выйти за пределы контейнера (container breakout).
2. Получить доступ к файловой системе ноды (например, прочитать `/etc/shadow` или `kubeconfig` кубелета).
3. Запустить процессы на уровне хоста и скомпрометировать весь кластер.

Для защиты от этого Kubernetes предлагает механизм **Pod Security Standards (PSS)**. Он определяет три стандарта (профиля) безопасности для подов:
- **`privileged`** — полностью открытый профиль. Разрешает всё, ограничения отсутствуют. Используется для системных компонентов (например, CNI плагинов, kube-proxy), которым нужен полный доступ к ноде.
- **`baseline`** — золотая середина. Запрещает явно опасные вещи (например, `hostNetwork`, монтирование `hostPath`), но позволяет многое для совместимости. Это минимально допустимый уровень для обычных приложений.
- **`restricted`** — максимально строгий профиль, следующий лучшим практикам безопасности (Best Practices). Требует запуска от непривилегированного пользователя, сброса всех capabilities ядра Linux и ограничения привилегий.

Механизм, который следит за выполнением этих стандартов и применяет их в кластере, называется **Pod Security Admission (PSA)**.
- PSA работает на уровне **namespace** через назначение специальных меток (labels).
- Доступны три режима применения политик: `enforce`, `warn`, `audit`.
- Проверка идёт на этапе **admission** (до записи объекта в etcd и тем более до старта контейнера) — нарушающий под отклоняется на уровне API-сервера.

**Профили по полям (что именно проверяется и почему это важно):**

| Контроль | privileged | baseline | restricted | Почему это важно? (Риск при нарушении) |
|----------|:---------:|:--------:|:----------:|-----------------------------------------|
| `privileged: true` | ✓ можно | ✗ | ✗ | Дает контейнеру права хостового root-пользователя. Легкий побег из контейнера. |
| `hostNetwork` / `hostPID` / `hostIPC` | ✓ | ✗ | ✗ | Позволяет прослушивать трафик ноды, убивать процессы ноды, взаимодействовать с IPC. |
| `hostPath`-тома | ✓ | ✗ | ✗ | Дает доступ к файловой системе хоста. Можно прочитать секреты других подов. |
| `runAsNonRoot: true` | — | — (можно root) | **обязателен** | Запрещает запуск процессов с UID 0 (root). |
| `allowPrivilegeEscalation: false` | — | — | **обязателен** | Запрещает процессам получать больше прав (например, через suid-бинарники). |
| `capabilities: drop [ALL]` | — | ограничены | **обязателен** (add только `NET_BIND_SERVICE`) | Убирает права Linux, такие как CHOWN, NET_ADMIN. Оставляет самый минимум. |
| `seccompProfile` (RuntimeDefault) | — | — | **обязателен** | Ограничивает системные вызовы (syscalls), которые может делать контейнер к ядру. |

**Включение профиля на namespace (YAML — это и есть `restricted-ns.yaml`):**

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: lab-restricted
  labels:
    pod-security.kubernetes.io/enforce: restricted        # БЛОКИРУЕТ нарушителей
    pod-security.kubernetes.io/warn: restricted           # предупреждает в kubectl (stderr)
    pod-security.kubernetes.io/audit: restricted          # пишет нарушение в audit-log
    # pod-security.kubernetes.io/enforce-version: v1.30    # (опц.) зафиксировать версию правил
```

> **Стратегия миграции** на боевом namespace: сначала включаем режимы `warn` + `audit`. В этом случае нарушители будут видны в логах аудита и в предупреждениях kubectl, но их создание не будет заблокировано. Затем мы чиним поды (добавляем `securityContext`). И только убедившись, что нарушений нет, включаем `enforce`. Так restricted профиль не уронит существующие workload'ы внезапно.

### PSA vs PSP — почему PSP убрали и что взамен

До k8s 1.21 ту же задачу решал **PodSecurityPolicy (PSP)** — admission-плагин с
объектами-политиками. PSP **deprecated в 1.21** и **удалён в 1.25**. В старых
кластерах вы его ещё встретите, поэтому важно понимать разницу:

| Характеристика | PodSecurityPolicy (PSP, удалён в 1.25) | Pod Security Admission (PSA, с 1.25) |
|---|---|---|
| Гранулярность | отдельный объект-политика (CRD) | 3 фиксированных встроенных профиля |
| Привязка | через сложный RBAC (кто имеет `use` на PSP) | простая метка на уровне namespace |
| Боль при дебаге | какой PSP применится — зависело от RBAC и сортировки по алфавиту, **непредсказуемо** | детерминированно: применяется профиль из метки namespace |
| Мутация подов | мог МУТИРОВать под (доставлять securityContext по умолчанию) | **только валидирует** на входе, не меняет ничего |
| Гибкость | можно писать произвольные комбинации правил | фиксированные профили (за кастомными правилами — идем к VAP) |

- **Почему отказались:** PSP было катастрофически трудно внедрить безопасно. При нескольких
  подходящих политиках выбор зависел от имён и RBAC, легко было «случайно» выдать
  привилегии разработчикам. Кроме того, PSP мутировал поды, что усложняло отладку (человек деплоил одно, а в кластер попадало другое).
- **Путь миграции PSP → PSA:** замапить свои старые PSP на ближайший из трёх профилей PSA;
  то, что не покрывается профилем (специфические правила, например, разрешить конкретный hostPath), вынести в
  **ValidatingAdmissionPolicy** (Часть 2) или Kyverno/Gatekeeper (Часть 3).
- PSA **НЕ мутирует** объекты. Если поду нужны дефолтные `securityContext` (например, чтобы запустить от `uid 1000`), разработчик должен сам прописать их в манифесте, либо их ставит
  отдельный mutating-механизм (LimitRange/webhook). PSA лишь проверяет итоговый результат.

---

**Цель Части 1:** Включить restricted профиль и наглядно увидеть, что легитимный под проходит, а опасный под отклоняется на корню.

**Используемые манифесты:** `manifests/restricted-ns.yaml`, `good-pod.yaml`,
`broken/scenario-01/bad-pod.yaml`.

---

### 1.1 restricted: good проходит, bad отклоняется

Создадим тестовые файлы, если их нет.

```bash
mkdir -p manifests broken/scenario-01

# Манифест Namespace
cat <<EOF > manifests/restricted-ns.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: lab-restricted
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/warn: restricted
    pod-security.kubernetes.io/audit: restricted
