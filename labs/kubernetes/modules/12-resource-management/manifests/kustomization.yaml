apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Детерминированная часть (QoS + PriorityClass). Preemption-демо (low/high-prio)
# и OOM применяются отдельно — им нужна разметка ноды / они специально падают.
resources:
- qos.yaml
- priorityclasses.yaml
