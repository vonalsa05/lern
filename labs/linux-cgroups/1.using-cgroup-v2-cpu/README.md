# Лабораторная работа: Управление CPU с использованием cgroup v2 в Linux

⏱ **Время на выполнение:** 15-20 минут
**Сложность:** Средняя

## Оглавление
1. [Подготовка](#подготовка)
2. [Шаг 1: Создание cgroup для ограничения CPU](#шаг-1-создание-cgroup-для-ограничения-cpu)
3. [Шаг 2: Запуск нагрузки](#шаг-2-запуск-нагрузки)
4. [Шаг 3: Добавление процесса в cgroup](#шаг-3-добавление-процесса-в-cgroup)
5. [Шаг 4: Проверка работы](#шаг-4-проверка-работы)
6. [Проверка модуля](#проверка-модуля)
7. [Уборка (Cleanup)](#уборка-cleanup)
8. [Troubleshooting — частые проблемы](#troubleshooting--частые-проблемы)
9. [Контрольные вопросы](#контрольные-вопросы)

## Подготовка

# База (узел 2 ядра 4 ГБ)

1. Обновите систему и установите необходимые пакеты:
    ```bash
    sudo apt update
    sudo apt install stress cgroup-tools htop
    ```

2. Проверьте доступные подсистемы cgroups:
    ```bash
    lssubsys -am
    ```

    Вы должны увидеть подсистему `cpu` в списке.

3. Проверьте, смонтирована ли система cgroups версии 2:
    ```bash
    mount | grep cgroup
    ```

    Вы должны увидеть монтированную файловую систему `cgroup2`.

## Шаг 1: Создание cgroup для ограничения CPU

1. Создайте новую cgroup для управления использованием CPU:
    ```bash
    sudo mkdir /sys/fs/cgroup/limited_cpu_group
    ```

2. Установите ограничение на использование процессорного времени. В данном примере процессам внутри группы будет доступно до 50 мс CPU времени на каждые 100 мс (что эквивалентно 50% CPU, файл принимает значения в микросекундах):
    ```bash
    echo "50000 100000" | sudo tee /sys/fs/cgroup/limited_cpu_group/cpu.max
    ```

## Шаг 2: Запуск нагрузки

1. Запустите процесс, который создаст нагрузку на CPU:
    ```bash
    stress --cpu 2 --timeout 240 &
    ```

2. Найдите PID запущенного процесса `stress`:
    ```bash
    ps aux | grep stress
    ```

    Скопируйте PID процесса из вывода команды.

## Шаг 3: Добавление процесса в cgroup

1. Добавьте PID процесса в созданную cgroup:
    ```bash
    echo <PID1> | sudo tee /sys/fs/cgroup/limited_cpu_group/cgroup.procs
    ```

    При необходимости добавьте и другие процессы, если их несколько:
    ```bash
    echo <PIDN> | sudo tee /sys/fs/cgroup/limited_cpu_group/cgroup.procs
    ```

## Шаг 4: Проверка работы

1. Используйте команду `htop` или `top` для наблюдения за тем, как процесс использует CPU:
    ```bash
    top
    ```

2. Проверьте, что процесс находится в нужной группе cgroups, выполнив:
    ```bash
    cat /sys/fs/cgroup/limited_cpu_group/cgroup.procs
    ```

## Проверка модуля

Для автоматической проверки правильности выполнения задания запустите скрипт верификации:

```bash
./verify.sh
```

Если скрипт сообщает об успешной проверке, значит вы всё сделали верно!

## Уборка (Cleanup)

После завершения работы необходимо очистить систему от созданных ресурсов и остановить нагрузочные процессы. Для автоматической очистки запустите скрипт:

```bash
./cleanup.sh
```

Либо выполните шаги вручную:
1. Завершите процессы `stress`.
2. Удалите созданную cgroup:
    ```bash
    sudo rmdir /sys/fs/cgroup/limited_cpu_group
    ```

## Troubleshooting — частые проблемы

- **Файл `cpu.max` отсутствует в созданной директории `limited_cpu_group`.**
  - *Решение:* Возможно, подсистема `cpu` не доступна в текущем дереве cgroup v2 (не добавлена в `cgroup.subtree_control` родительской группы) или ОС загружена с параметрами, отключающими cgroup v2. Проверьте содержимое файлов `cat /sys/fs/cgroup/cgroup.controllers` и `cat /sys/fs/cgroup/cgroup.subtree_control`.

- **Команда `rmdir /sys/fs/cgroup/limited_cpu_group` выдает ошибку `Device or resource busy`.**
  - *Решение:* Это означает, что в cgroup всё ещё есть запущенные процессы. Проверьте содержимое файла `cat /sys/fs/cgroup/limited_cpu_group/cgroup.procs` и завершите эти процессы (например, командой `kill -9 <PID>`), после чего повторите удаление директории.

- **Процесс `stress` не реагирует на ограничение и потребляет 100% CPU.**
  - *Решение:* Убедитесь, что вы добавили именно PID рабочих процессов `stress` (worker процессов, непосредственно потребляющих CPU), а не основного родительского процесса или оболочки. Проверьте это с помощью `cat /sys/fs/cgroup/limited_cpu_group/cgroup.procs`.

## Контрольные вопросы

1. В чем основное архитектурное отличие между cgroup v1 и cgroup v2 при управлении ресурсами?
2. Что означают два числа (например, `50000 100000`) в файле `cpu.max` и как они соотносятся с физическими процессорными ядрами?
3. Что произойдет, если процесс, находящийся в cgroup с ограничением CPU, породит дочерний процесс? Будет ли дочерний процесс также ограничен в ресурсах?
4. В чем разница между файлами `cgroup.procs` и `cgroup.threads` внутри директории cgroup?
