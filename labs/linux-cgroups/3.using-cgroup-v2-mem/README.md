# Лабораторная работа: Ограничение использования оперативной памяти с использованием cgroups

**⏱ Время выполнения:** ~15 минут
**Сложность:** Средняя

## Оглавление
1. [База](#база)
2. [Шаг 1: Создание группы cgroup для ограничения памяти](#шаг-1-создание-группы-cgroup-для-ограничения-памяти)
3. [Шаг 2: Запуск процесса с помощью stress](#шаг-2-запуск-процесса-с-помощью-stress)
4. [Шаг 3: Изменение лимита и повторный тест](#шаг-3-изменение-лимита-и-повторный-тест)
5. [Troubleshooting — частые проблемы](#troubleshooting--частые-проблемы)
6. [Проверка модуля](#проверка-модуля)
7. [Контрольные вопросы](#контрольные-вопросы)
8. [Уборка](#уборка)

## База

1. Установите необходимые утилиты:
    ```bash
    sudo apt update
    sudo apt install stress cgroup-tools
    ```

2. Проверьте, что система использует cgroup версии 2:
    ```bash
    mount | grep cgroup2
    ```

## Шаг 1: Создание группы cgroup для ограничения памяти

1. Создайте новую группу cgroup для ограничения использования оперативной памяти:
    ```bash
    sudo mkdir /sys/fs/cgroup/memory_limit_group
    ```

2. Установите лимит на использование оперативной памяти. Например, установим лимит в 500 MB:
    ```bash
    echo "500M" | sudo tee /sys/fs/cgroup/memory_limit_group/memory.max
    ```

3. Проверьте, что лимит был установлен:
    ```bash
    cat /sys/fs/cgroup/memory_limit_group/memory.max
    ```

## Шаг 2: Запуск процесса с помощью `stress`

1. Запустите процесс с помощью утилиты `stress`, который создаст нагрузку на оперативную память (например, выделит 1 GB памяти):
    ```bash
    stress --vm 1 --vm-bytes 1G --timeout 60 &
    ```

2. Найдите PID запущенного процесса (обратите внимание на PID рабочего процесса stress):
    ```bash
    ps aux | grep stress
    ```

3. Добавьте процесс в созданную группу cgroup, чтобы ограничить его использование памяти:
    ```bash
    echo <PID> | sudo tee /sys/fs/cgroup/memory_limit_group/cgroup.procs
    ```
    Замените `<PID>` на фактический PID процесса.

4. Если необходимо завершить процесс до таймаута, используйте команду `kill`:
    ```bash
    kill <PID>
    ```

## Шаг 3: Изменение лимита и повторный тест

1. Установите новый лимит для использования оперативной памяти, например, 200 MB:
    ```bash
    echo "200M" | sudo tee /sys/fs/cgroup/memory_limit_group/memory.max
    ```

2. Запустите процесс, который попытается использовать больше памяти (например, 3 GB):
    ```bash
    stress --vm 1 --vm-bytes 3G --timeout 120 &
    ```

3. Найдите PID процесса и добавьте его в cgroup:
    ```bash
    echo <PID> | sudo tee /sys/fs/cgroup/memory_limit_group/cgroup.procs
    ```

4. Если процесс превышает лимит по памяти, OOM Killer системы может его завершить (Out Of Memory). Вы можете проверить события OOM Killer с помощью команды:
    ```bash
    dmesg | grep -i oom
    ```

## Troubleshooting — частые проблемы

- **Ошибка "Device or resource busy" при попытке удалить cgroup:**
  Это означает, что в cgroup все еще запущены процессы. Выведите список процессов (`cat /sys/fs/cgroup/memory_limit_group/cgroup.procs`) и завершите их с помощью `kill -9 <PID>` перед удалением директории.
- **`memory.max` не применяется (Permission denied):**
  Убедитесь, что вы выполняете запись в файл от имени суперпользователя (например, с использованием `sudo tee`).
- **Процесс `stress` не убивается OOM Killer-ом:**
  Если на машине активен файл подкачки (swap), процесс может использовать его вместо того, чтобы быть убитым OOM Killer-ом. Вы можете ограничить использование swap в cgroup через файл `memory.swap.max`.

## Проверка модуля

Чтобы убедиться, что вы успешно выполнили работу и создали cgroup с лимитами, запустите скрипт проверки:
```bash
./verify.sh
```

## Контрольные вопросы

1. В чем разница между `memory.max` и `memory.high` в cgroup v2?
2. Что произойдет, если процесс в cgroup попытается выделить больше памяти, чем указано в `memory.max`, и swap отключен?
3. Каким образом можно ограничить использование swap для конкретной группы cgroup?
4. Могут ли процессы внутри cgroup самостоятельно изменять свои лимиты `memory.max`? Почему?

## Уборка

По завершению лабораторной работы выполните скрипт очистки для удаления созданной cgroup и остановки тестовых процессов:
```bash
./cleanup.sh
```
